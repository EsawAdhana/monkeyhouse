rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Block all client access - Admin SDK bypasses these rules
    match /{document=**} {
      allow read, write: if false;
    }
  }
} 